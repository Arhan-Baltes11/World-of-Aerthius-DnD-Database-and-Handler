using MySql.Data.MySqlClient;

namespace AerthiusConsole;

public class World
{
    /* 
        The world is where we hold all of the available data on the world.
        It would have different realms, all containing different historical figures,
        with those realms holding historical figures.

        When the project is started, the world is first initialized, with each of the realms
        bringing characters and locations based on where each of the realms are.
    */

    public void InitializeWorld(MySqlConnection conn)
    {

    }
}